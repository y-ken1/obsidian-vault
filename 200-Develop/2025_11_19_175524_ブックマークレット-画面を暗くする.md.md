---
aliases:
  - ブックマークレット
tags:
  - ブックマークレット
created:
  - 2025-11-19 17:55:24
---

# ブックマークレット-画面を暗くする


前面に黒（透過度付き）のdivを設定する。
背後のクリックは可能

```JavaScript
javascript:(function(){
  const ALPHA = 0.8; // 透明度の定数
  let o = document.getElementById('bmOverlay');
  if (o) {
    o.remove();
  } else {
    o = document.createElement('div');
    o.id = 'bmOverlay';
    o.style.position = 'fixed';
    o.style.top = 0;
    o.style.left = 0;
    o.style.width = '100%';
    o.style.height = '100%';

    // 背景色で暗くする
    o.style.backgroundColor = `rgba(0,0,0,${ALPHA})`;

    // 背後のクリックを通す
    o.style.pointerEvents = 'none';

    o.style.zIndex = 999999;
    document.body.appendChild(o);
  }
})();
```

１行版　（これをブックマークのURLにする）
```JavaScript
javascript:(function(){const ALPHA=0.8;let o=document.getElementById('bmOverlay');o?o.remove():(o=document.createElement('div'),o.id='bmOverlay',o.style.position='fixed',o.style.top=0,o.style.left=0,o.style.width='100%',o.style.height='100%',o.style.backgroundColor=`rgba(0,0,0,${ALPHA})`,o.style.pointerEvents='none',o.style.zIndex=999999,document.body.appendChild(o));})();
```