- [x] 青ホームページのgit管理からPDFフォルダを除く 🛫 2025-11-11 📅 2025-11-11 ✅ 2025-11-11

## 目的

青ホームページをGitHub管理にしたいが、PDFは除きたい。
そのために、過去の履歴も含めてpublic/assets/pdf/をgit管理外にする

ついでに修正依頼の管理外にしてます

### 事前準備（git-filter-repoのインストール）
```bash
# 1. 保存先フォルダを作る（なければ）
mkdir -p ~/.local/bin

# 2. git-filter-repo をダウンロード
curl -o ~/.local/bin/git-filter-repo https://raw.githubusercontent.com/newren/git-filter-repo/main/git-filter-repo

# 3. 実行権限を付与
chmod +x ~/.local/bin/git-filter-repo

# 4. パスを通す（初回だけ）
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc

# 5. 動作確認
git filter-repo --version

# ここでPythonと出る場合はviで
# ~/.local/bin/git-filter-repoの1行目を
# 「python3」->「python」に編集する

## before
#!/usr/bin/env python3

## after
#!/usr/bin/env python

vi ~/.local/bin/git-filter-repo
```



### 履歴も含めてgit管理外にする手順

PDF をバックアップ（ワークツリーに残したい場合）
```bash
mkdir -p ~/pdf_backup
cp -r public/assets/pdf/* ~/pdf_backup/
```

履歴から PDF フォルダを削除（強制）        
**※この時、実体のファイル（フォルダ）も削除されます。必ずバックアップすること**

```bash
git filter-repo --path public/assets/pdf/ --invert-paths --force
```


PDF フォルダを無視する設定
```bash
echo "public/assets/pdf/" >> .gitignore
git add .gitignore
git commit -m "Ignore public/assets/pdf folder"
```

手元に PDF を復元
```bash
cp -r ~/pdf_backup public/assets/pdf/
```


 確認
```
git status         # Git が PDF を追跡していないことを確認
ls public/assets/pdf/  # ファイルが手元に残っていることを確認
```

 